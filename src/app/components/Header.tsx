/* Instruments */
import { Nav, styles } from "./MainImports";

export const Header = () => {

    return (
        <header className={styles.header}>
            <Nav />
            {/* <img src="/logo.svg" className={styles.logo} alt="logo" /> */}
        </header>
    );
};
